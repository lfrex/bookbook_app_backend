<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Bookbook Book Display Page</title>
        <link rel="stylesheeet" href="/css/app.css"/>
    </head>
    <body>
        <h1>Book Display Page</h1>
        Title: <%= book.title %>
        <br>
        Author: <%= book.author %>
        <br>
        Status: <% if (book.isRead === true) { %>
            Already read.
        <% } else { %>
            Pending to read.
        <% } %>
        <br>
        <br>
        <img src="<%= book.img %>" alt="book cover" />
        <br>
        <br>
        Comments:
        <br>
        <br>
        <%= book.comments %>
        <br>
        <br>
        <form action="/books">
            <input type="submit" value="Return to index" />
        </form>
    </body>
</html>